{
  "$schema": "https://react.doctor/schema/config.json",
  "rules": {
    // Off: dispara falsos positivos en acciones puras (preview/test email) que no tienen caché que invalidar.
    "react-doctor/query-mutation-missing-invalidation": "off"
  }
}
